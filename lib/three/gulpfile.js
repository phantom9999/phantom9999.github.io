const fs=require("fs"),del=require("del"),gulp=require("gulp"),concat=require("gulp-concat"),uglify=require("gulp-uglify");gulp.task("clean",()=>{del(["*.min.js","!three.min.js"])}),gulp.task("scripts",()=>{const e=fs.readdirSync("src");e.forEach(e=>{e=e.split(".")[0];return gulp.src([`src/${e}.js`,"lib/*.js"]).pipe(concat(`${e}.min.js`)).pipe(uglify()).pipe(gulp.dest("./"))})}),gulp.task("default",["clean","scripts"]);